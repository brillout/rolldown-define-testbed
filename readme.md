Showcase that Rolldown's `define` option doesn't apply to local variables but only global variables.

Same use case but with different tools:
 - `esbuild/define`
 - `rolldown/replacePlugin`
 - `rollup/replacePlugin`
